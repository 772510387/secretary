# AI Scripts

AI 协作辅助脚本。

## 未来脚本

- `collect-context.ts`：按模块收集上下文。
- `new-module-readme.ts`：生成模块 README 模板。
- `check-agent-rules.ts`：检查是否遗漏 README 或测试说明。
- `summarize-change.ts`：生成改动摘要。

## 目标

让 vibe coding 更稳定：每次让 AI 实现单点能力时，都能快速加载同一份上下文和验收标准。

