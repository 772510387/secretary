# API Interface

负责未来 HTTP API。

## 需要实现

第一阶段可暂缓。未来需要：

- 账户查询 API。
- 行情查询 API。
- 报告查询 API。
- 交易意图创建 API。
- 模拟订单 API。
- 记忆提案 API。
- 系统状态 API。

## 安全要求

- API 默认只监听本机。
- 写操作需要鉴权。
- 实盘操作需要额外确认。
- 所有写操作进入审计日志。

